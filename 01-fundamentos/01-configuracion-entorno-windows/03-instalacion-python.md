## 🐍 Instalación de Python en Ubuntu (WSL)

### 1. Instalar software requerido
Este paquete permite gestionar repositorios adicionales.

```bash
sudo apt install software-properties-common
```
### 2. Agregar repositorio de versiones recientes (deadsnakes)

```bash
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
```

### 3. Instalar Python (elige la versión)

```bash
sudo apt install python3
```

