## 🧩 ¿Que es pip3?

`pip3` es el manejador de paquetes más utilizado en Python. Con él podrás instalar librerías esenciales como **NumPy** o **Pandas**, fundamentales para tu trabajo diario en ciencia de datos.

---

## 📦 Instalación de pip3

En la terminal de ubuntu ejecuta los siguientes comandos:

### 1. Actualizar el sistema operativo Ubuntu

```bash
sudo apt update
```

### 2. Instalar pip3

```bash
sudo apt-get -y install python3-pip
```

### 3. Verificar la instalación

```bash
pip3 --version
```
